import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    width: "100%",
  },
  img: {
    height: "2rem",
  },
  support: {},
}));

export default useStyles;
